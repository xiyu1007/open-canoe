[project]
name = "open-canoe"
version = "0.3.0"
description = "Open CAN bus analyzer with STM32 hardware probe"
requires-python = ">=3.11"

dependencies = [
    "pyserial>=3.5",
    "pydantic>=2.0",
    "pyyaml>=6.0",
]
